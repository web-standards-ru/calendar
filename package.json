{
  "private": true,
  "scripts": {
    "start": "npm run generate && npm run publish",
    "generate": "node scripts/generateCalendar.js",
    "publish": "git add dist/calendar.ics && git commit -m 'Публикует календарь' && git push"
  },
  "devDependencies": {
    "ical-generator": "^0.2.9",
    "js-yaml": "^3.7.0",
    "pify": "^3.0.0"
  }
}
